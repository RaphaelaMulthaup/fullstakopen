```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: The submit event is intercepted (e.preventDefault())
    Note right of browser: The new note is pushed to the local 'notes' array (State)
    Note right of browser: redrawNotes() is called to update the UI immediately

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Server saves the new note to the database
    server-->>browser: HTTP 201 Created (Confirmation JSON)
    deactivate server

    Note right of browser: The callback logs the server's confirmation message